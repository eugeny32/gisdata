/**
 * Кватернион -90° по X — единый для обоих типов моделей. Для сплатов это
 * эмпирически подобранный рабочий поворот. Для LAS он переводит исходные
 * геодезические координаты (X=север, Y=восток, Z=вверх) в Y-up мир, в
 * котором живёт камера/орбита этого плеера — поворот на -90° по X
 * отображает старое Z (вверх) в новое Y (вверх).
 */
export const AXIS_FIX_ROTATION: readonly [number, number, number, number] = [-0.7071, 0, 0, 0.7071];
